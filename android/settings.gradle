

pluginManagement {
    repositories {
        gradlePluginPortal()
        google() // <-- Make sure this is present
        mavenCentral() // <-- Make sure this is present
    }
}

rootProject.name = "Javanese2000"
include(":app")